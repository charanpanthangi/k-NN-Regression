# Examples

Run the main script to train and evaluate the k-NN regressor on the California Housing dataset:

```bash
python app/main.py
```

After running, check the `outputs/` folder for SVG plots of predicted vs actual values and residuals.
